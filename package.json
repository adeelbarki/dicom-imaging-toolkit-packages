{
  "name": "rtstruct-js",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DICOM RT Structure Set reading and writing, validated against analytic phantoms.",
  "scripts": {
    "pretest": "npm run check:deps",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check:deps": "node scripts/check-dependency-rule.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
